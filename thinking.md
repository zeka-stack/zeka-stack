# 思考碎片

## AI 友好的接口文档

现在传统的 Swagger 文档是给人的看的，而不是给 AI 助手的, 所以是不是应该优化一下 Swagger 文档? 让它更适合 AI 助手

## 为框架接入 AI 能力

现在 AI 已经不是“辅助写代码”那么简单了，它已经覆盖原型设计、UI 协作、测试、文档、运维定位等环节。  
如果想让这套脚手架的生命周期更长，我认为核心不是再加几个 AI 小功能，而是把框架本身升级成**AI 可协作、可理解、可执行的工程底座**。

我现在的想法至少有几层：

### 1. 给 AI 一套稳定的开发规范
不只是给人写规范文档，而是要专门给 AI 准备 rules、playbook 和约束说明。  
比如 AI 生成一个新 starter 时，它应该知道：
- 目录结构该怎么拆（autoconfigure / core / starter）
- 命名约束是什么
- 异常、响应、日志、分层应该怎么写
- 文档、示例、契约模板要一并产出
- 哪些改动必须人工确认，哪些可以自动完成

也就是说，未来很多规范文档，本质上是**给人和 AI 同时看的操作手册**。

### 2. 把重复研发流程固化成 skills
我现在越来越倾向把“常见但容易出错”的流程做成可复用 skill，比如：
- 新增一个 starter
- 补齐示例工程和文档
- 生成契约与接口说明
- 生成测试骨架
- 检查依赖边界和分层违规
- 跑发布前检查

这类 skill 的目标不是让 AI 自由发挥，而是让 AI 按照 Zeka Stack 的节奏一步步执行。  
换句话说，未来脚手架不仅提供代码模板，还要提供**工作流模板**。

### 3. 让后端契约对 AI 更友好
当前 Swagger/Knife4j 更多是给人调试接口用的，AI 并不总是能从里面拿到足够上下文。  
我会继续考虑“双轨文档”：
- 一条给人看
- 一条给 AI 看

给 AI 的那份，不只是接口定义，最好还能带上字段语义、错误码、状态流转、输入输出样例，甚至页面动作和前端生成元数据。  
这个方向如果做得好，后面 AI 生成前端页面、生成调用代码、生成测试用例都会更稳。

### 4. 把 AI 接进监控和运维链路
这部分是我最近越来越在意的方向。  
比如：
- 启动异常时，AI 自动分析配置和依赖
- 报错日志进来后，AI 先尝试根据代码与上下文做初判
- 结合 APM / tracing / health endpoint 做故障定位
- 自动生成问题摘要、影响范围、可能根因、建议修复动作
- 只有超过某个阈值，才把事件推送给人处理

也就是说，AI 不只参与开发，还能参与“运行期协作”。  
如果这一层落地，框架的价值就不只是写代码更快，而是整个工程生命周期都在变短。

### 5. 为 AI 协作建立评测和约束体系
我越来越觉得，AI 不怕能用，怕的是不稳定。  
所以后面如果认真做，还需要补一套评测机制，比如：
- AI 生成的模块是否符合分层规范
- AI 生成的接口是否满足统一响应和错误约定
- AI 生成的测试是否覆盖关键路径
- AI 操作是否越权、是否缺少审计
- AI 产出是否满足文档和契约要求

没有评测，AI Native 很容易变成 demo；有了评测，它才可能变成真正可持续的工程能力。

### 6. 其他我还没完全想清楚的方向
- AI 如何理解领域知识，不只是代码结构，还有业务语言、对象关系、状态机、错误码体系
- AI 如何参与配置治理，比如解释配置变更、评估风险、对比环境差异
- AI 如何参与发布协作，比如自动整理版本影响、升级风险、变更说明
- MCP / Tool / Agent Runtime 的边界应该怎么划，避免把业务系统和 AI Runtime 绑死

总的来说，我越来越倾向于把 Zeka Stack 从“写代码的脚手架”升级成：
- 给开发者的工程规范平台
- 给 AI 的协作规范平台
- 给团队的契约与文档平台
- 给运行期系统的 AI 运维协作平台

一句话：**不是简单接入 AI，而是重新设计框架与 AI 的协作方式。**
